using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoalBase : MonoBehaviour {

    /*
     * Every level should have at least a goal, with logic to test if game win or lose
     * But when to detect leave to each game.
     * This structure serve toclearify the code cleaness
     */

    public virtual void CheckTerm() { 
    
    }

}
